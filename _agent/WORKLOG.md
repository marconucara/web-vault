# Agent Worklog

Append one row per commit. Newest at the bottom.

| Date | Commit | Branch | Item | Notes |
|------|--------|--------|------|-------|
| 2026-07-29 | v0.1.0 | main | bootstrap + backfill | Initial import: web-vault framework package + docflow catalogue (ADRs 0001–0029, all Implemented except 0029 Accepted). Signed, tagged v0.1.0. Distribution validated: installs as a git dependency and the reference consumer builds green on Cloudflare (preview + production). |
| 2026-07-30 | v0.2.0 | main | ADR 0029 (0001+0002) | Shipped agent-driven onboarding (SETUP.md), user-facing README, single-source DEPLOY.md; product name WebVault; AGPL-3.0. Build now generates `dist/_headers` (ADR 0026 r2, consumer-overridable) instead of scaffolding it. ADR 0029 → Implemented; plan 0001/0002 → done. Squash+ff to main, tagged v0.2.0. Onboarding UX validated on a clean vault. |
| 2026-07-30 | v0.3.0 | main | dev editor interop fix | Fixed `wv dev` blanking on plain-CJS transitive deps (style-to-js via react-markdown; use-sync-external-store via zustand/BlockNote): pre-bundle the affected libraries in Vite `optimizeDeps.include`. Dev-only; Rollup build unaffected. Verified in-browser on a real install. See `plan/done/2026-07-30-dev-editor-cjs-interop.md`. |
| 2026-07-30 | 83b7c2d…05e444b | main | ADR drafts 0030–0034 | Drafted, as Proposed: 0030 background freshness detection, 0031 edit-time drift policy (warn-only), 0032 dual-format views (`.base` + `.yml`) with dedup, 0033 built-in vault-independent sidebar views, 0034 client-side settings modal. Decisions only; no implementation in these commits. |
| 2026-07-30 | e993aed, c8aba90 | main | ADR 0035 draft + README | Drafted 0035 (second delivery path — Cloudflare template onboarding). README gained a Why/motivation section and no-vault guidance. |
| 2026-07-30 | ef96c81, 7f60ec0, 0f1497c | main | ADRs 0033 + 0036 | Implemented built-in sidebar views (All notes, Inbox, Shared — vault-independent) and local dev edit persistence: `wv dev` now writes edits to disk, no commit. See `plan/done/2026-07-30-builtin-sidebar-views.md`, `plan/done/2026-07-30-local-dev-edit-write-to-disk.md`. |
| 2026-07-30 | 6eb52b1, 9193b6a, 13d7c11 | main | ADR drafts 0037–0039 | Drafted, as Proposed: 0037 versioning and release policy (semver, 1.0.0, GitHub Releases), 0038 in-app upgrade notice, 0039 adopter upgrade path. All three still Proposed — 0038 and 0039 depend on 0037 landing first. |
| 2026-07-31 | 1910f81 | main | dev attachments fix | The dev server now serves the vault's `/attachments/*`, which it previously 404'd. |
| 2026-07-31 | v0.4.0 (975b74e), 4dc957c | main | ADR 0040 — Workers substrate | Migrated the Cloudflare substrate from Pages to Workers; 0040 supersedes 0026. `functions/worker.js`, `scripts/generate-worker.mjs`, `wv build` wiring; Pages `generate-functions.mjs` removed. DEPLOY.md rewritten for Workers; marconucaravault migrated to the Workers shape. Tagged **v0.4.0** — the current release tag. See `plan/done/2026-07-31-cloudflare-workers-deploy.md`. |
| 2026-07-31 | 0f8b2ae…9fc2a4f | main | DEPLOY.md hardening (post-tag) | Documented the `../*` build watch path for the `.web` layout, the Workers Builds build API token + troubleshooting, the build-scoped `MAP_CACHE_KEY` + `SITE_URL` for the map cache, and the `/maps-cache.json` Access Bypass row. Docs only. |
| 2026-07-31 | 6d5a344, c3098da | main | ADR 0035 — template, tried and reverted | Moved the starter template into `templates/base/`, then reverted it: the "Deploy to Cloudflare" button rejects a subfolder with no root `wrangler.toml`, failing before the screen where **Path** is set. ADR 0035 gained acceptance criteria 6 and 7 and returned to Accepted pending `plan/todo/0001`. |
| 2026-07-31 | d81628a, d708c54, 5c9160e | main | ADR 0041 — quality gate | Added the automated quality gate: `yarn verify` = `yarn typecheck` + `yarn test`. 0041 → Implemented. See `plan/done/2026-07-31-automated-quality-gate.md`. |
| 2026-08-01 | c938953 | main | ADR 0035 closure (plan todo/0001) | Closed the starter-template item. The `web-vault` side shipped earlier in `c3098da` (deleted `templates/base/`, README repointed); this closes it now that `marconucara/web-vault-template` is restored as the one-click entry point and a live button test from its root reaches the configuration screen. ADR 0035 → Implemented (r5), INDEX updated, plan → `done/2026-08-01-template-separate-repo.md`. Also swapped the two post-deploy steps in the template's `README.md` and `welcome.md` so Access (make it private) comes before `GITHUB_TOKEN` — the site is public until Access is on (template `fe085ff`). |
| 2026-08-01 | v0.5.0 | main | release v0.5.0 + `_agent` realignment | Tagged **v0.5.0**, gathering everything on `main` since `v0.4.0`: the ADR 0041 quality gate, DEPLOY.md hardening, and the template/ADR bookkeeping. No user-facing features or bug fixes — strictly a patch by semver, cut as a minor rather than leaving `main` untagged. Bumped the adopter pin to `#v0.5.0` in `SETUP.md` (it still said `v0.3.0`, two releases stale), the `blob/` link in `README.md`, and `package.json`. Backfilled the WORKLOG rows missing between `v0.3.0` and here, and rewrote `CURRENT_FOCUS.md`, which still claimed "all ADRs 0001–0029 Implemented" and named `v0.3.0` as current. |
| 2026-08-02 | v0.5.1 | main | ADR 0028 r2 — maps resolver cache poisoning | Fixed a rate-limited Google response being cached as a resolved place. `fetchPage` returned the last `429` after exhausting retries; `extractCoords` then matched `@lat,lng` inside the `/sorry/` page's `continue=` parameter, and both the retry filter and the usable guard accepted `lat != null` as success — so the bogus entry entered `dist/maps-cache.json` and was never retried (permanent until a `MAP_CACHE_KEY` rotation). Now: blocked responses are failed fetches, `isUsableEntry` requires title-or-image, unusable entries stay out of both caches and are re-fetched, and unresolved links log `UNRESOLVED` without failing the build. Pre-fix poisoned caches self-heal on the next build. Deliberately NOT a build-failure gate: a `429` is transient and hits every link at once. Client-side surfacing (`mapsIssues`) parked as an ADR 0028 open question. First tests for `scripts/`. See `plan/done/2026-08-02-maps-resolver-reject-blocked-responses.md`. |
| 2026-08-03 | v0.5.4 | main | leaflet dev pre-bundle + ADR 0028 r3 | Fixed the map view failing to open under `wv dev` with `does not provide an export named 'default'`. `leaflet` is a UMD bundle Vite cannot statically read exports from, and `optimizeDeps.include` did not list it; because `MapView.jsx` is lazily imported the dev scanner never encountered it at startup, so it was served raw the first time anyone switched to that view. Production (Rollup) was never affected. Also ADR `0028` r3: r2's "coordinates alone are not a place" governed the resolver and its caches, but read as a statement about which links are supported. Criterion 7 now says what the reader already did — every Maps link survives into the note, and missing photo/name/coordinates degrade the card rather than dropping the link. Four `collectPoints` tests pin it. Found while writing the starter notes: emphasis wrapped around an inline code span is split on export (`**a `c`** b` loses the emphasis on the code span) — logged as `plan/todo/0004`, welcome note reworded around it. |
| 2026-08-03 | 28d6864, 8546dc0, faf4b7b, 35838df | main | docs pass + routing handoff | Repointed the starter note's links to WebVault's own docs at `main` rather than a tag (a reader wants current instructions; pinning meant five link targets to bump per release across two repos), keeping the version only where it describes an executed dependency. Restructured `README.md` to lead with the product rather than the architecture, and re-synced the welcome fixture after it. Also filed `_agent/prompts/routing-adr.md`: a heading link inside a note lands nowhere because the read-only renderer emits no heading ids — filed as a handoff on the reasoning that the fix is a decision, not a patch. That left it invisible to `INDEX.md` and the queue; the 2026-08-05 audit promoted it to `plan/todo/0006`. |
| 2026-08-05 | 4c2e315 | main | dev server watches the vault | `wv dev` generated `.wv/content.json` once and then started Vite, so a vault edit stayed invisible until restart. Added the missing half of the loop — a dev-only plugin watching notes, `views/` and `attachments/`, debounced and serialised, leaving the previous content in place and the server up when a note fails to parse. Walk exclusions moved to `scripts/content-ignore.mjs` so watcher and walker share them without importing `build-content.mjs` (which generates on import). Regeneration spawns the script as a subprocess so dev output stays byte-identical to `wv gen`/`wv build` rather than growing a second pipeline. Dev-only; build and preview unaffected. No version bump — rides along with a later release. See `plan/done/2026-08-05-dev-server-vault-watch.md`. |
| 2026-08-05 | v0.6.0 | main | ADR 0042 — brand identity shipped | Placed the externally-produced icon set into the product. `brand/` now ships with the package (added to `files`), and `scripts/copy-brand.mjs` copies it into `dist/` at build time with the consumer's `public/` winning per file — the same package-owns-the-boilerplate shape as `build-headers.mjs`, chosen because Vite's `publicDir` points at the CONSUMER's `.web/public` and cannot ship framework assets. A `brand-dev` middleware serves the same URLs under `wv dev`. The share pages and the 404 could not simply link the root icon: `/shared/*` is the only Access-bypassed prefix, so the shares reference one shared copy at `dist/shared/favicon.svg`, and the 404 — served for unmatched paths at any depth — inlines the mark as a `data:` URI. All three read `brand/favicon.svg` at build time, so they cannot drift (ADR 0042 criterion 8). The sidebar's plain `Vault` text became the mark plus the wordmark `WebVault` (`src/components/BrandMark.jsx`, `--wv-brand` added to `styles.css`, ink on `currentColor` so one drawing covers both themes). ADR 0042 r4 closes the raster open question — the set is committed pre-rendered, not generated, keeping an image dependency out of a gate that must run from a bare clone — and advances to Implemented. Verified outside the gate against a throwaway vault: real build, static server, and a live `wv dev`. 68 → 70 tests. See `plan/done/2026-08-05-brand-icons-and-sidebar-mark.md`. |
| 2026-08-06 | 2fc804f, 1243c86 | main | ADR 0015 — code fence shape (plan todo/0005) | Fixed a code fence being rewritten by an open-and-save cycle. Started as "an unlabelled fence gains `text`" and turned out to be four symptoms of one cause: a BlockNote code block keeps only its `language`, so the marker kind, the marker length, the absence of an info string and the indent are all dropped on parse and re-invented by the exporter (always three backticks, at column zero, labelled `text`). The `text` case was the unrecoverable one — it is BlockNote's default, so a bare fence and one that really declares `text` parse to the *same* block; the fix the plan item first proposed, in `normalizeMarkdown`, could not have worked because by that stage the distinction no longer exists. All four now ride across the parse in the one field that survives it: the opening fence is rewritten to a `wv-fence-<kind><len>i<indent>-<info>` sentinel language and restored after export — a fourth pre/post pair beside the wikilink, media-link and map-link ones. The indent could not be repaired later: `blocksToHTMLLossy` lifts every non-list child out of its `<li>` and records the depth only in a `data-nesting-level` attribute that `htmlToMarkdown` never reads, and in the emitted markdown a fence that *follows* a list is indistinguishable from one nested *inside* it. That flattening hits every non-list child type (paragraph, blockquote, table, heading) and, unlike the fence rewrite, changes the document rather than only its bytes — filed as `plan/todo/0004`, where the general fix means replacing `blocksToMarkdownLossy` across every export path. Plan item rewritten rather than patched: its title, diagnosis and criteria all described the narrower defect (renamed `0005-fence-shape-lost-on-round-trip`). TypeScript was considered and declined for the module — the repo is JS under `checkJs`, no `.ts` files exist, and introducing the first is an ADR-level decision; the data shapes touched are pinned with JSDoc instead. ADR 0015 r5 (already `Implemented`, no status change); the residual flattening is recorded there as a known defect and criterion 5 states the general rule the four symptoms shared. 78 → 91 tests, 15 confirmed failing against the unmodified tree. See `plan/done/2026-08-06-fence-shape-lost-on-round-trip.md`. |
| 2026-08-06 | 1f81806, v0.6.1 | main | ADR 0015 r6 — nested block indent (plan todo/0004) | Fixed a block nested under a list item being exported at column zero. Unlike the fence rewrite that preceded it, this changed the *document*: the indent is what holds the block inside the item, so a paragraph, blockquote, table or heading left the list and a following ordered item restarted its numbering. `blocksToHTMLLossy` lifts every non-list child out of its `<li>` and keeps the depth only in a `data-nesting-level` attribute the markdown step never reads, so nothing downstream could recover it. The item expected to replace `blocksToMarkdownLossy`; that proved unnecessary — each block exports correctly on its own and only the composition was wrong, so `exportBlocks` walks the block tree and indents each child under its parent, calling the stock exporter on the parts. Three constraints, all found by testing rather than reading: a run of items goes through the exporter in ONE call (per-item calls restart every ordered list at 1); the run stops at a change of list type (the exporter separates two lists with a blank line, so one emitted line stops pairing with one item — the first implementation paired them regardless and DROPPED the second list outright, content loss caught by diffing edge cases against the unmodified tree, not by a failing test; a length guard now falls back to verbatim output, losing the indent rather than content); and a child indents to the width of the marker actually emitted, four for `10. `. Also fixed the checklist case, cause separate and in the dependency: BlockNote's markdown parser counts a task item's content column including the `[ ] ` marker (`markdownToHtml.ts`, `contentIndent`), so a child at the idiomatic column 2 is read as a sibling, while the same child under a plain bullet nests correctly — such children are now re-indented to the column the parser expects before the parse and put back after. Two parts of the fence item folded in: the sentinel no longer carries the fence indent (the walk applies it, carrying it too applied it twice) and `compactTableLine` keeps the leading whitespace it used to trim. Left as-is and confirmed pre-existing: a blank line before a list nested under a checklist item is dropped (spacing, not structure). ADR 0015 r6 — criterion 6 added for nesting depth, criterion 5 narrowed to the fence properties the sentinel still carries, and the r5 known defect closed. 91 → 121 tests, 16 confirmed failing against the unmodified tree. Verified against a real vault by the owner. See `plan/done/2026-08-06-nested-blocks-flattened-on-export.md`. |
| 2026-08-06 | 25135db | main | ADR 0025 — share-page task lists (plan todo/0005) | Fixed a checkbox list rendering on a public share page with **both** a bullet and a checkbox. The share page is built by the standalone renderer, which carries its own inline copy of the stylesheet — a deliberate duplicate, since the page must not pull in the app bundle. That copy styled `.markdown ul`/`li` generically and had no rule for the `contains-task-list` / `task-list-item` classes `remark-gfm` emits, so nothing suppressed the `ul` marker and the browser painted the disc alongside the checkbox, with the label misaligned against plain bullets in the same list. Four CSS rules, no change to the renderer's logic: the markup was already correct — `checked` and `disabled` are both emitted — so the read-only guarantee and the done/not-done state were never the defect, only the missing rules were; the tests assert them anyway since nothing else pinned them. Two cases the plan item did not anticipate, both found by reading the renderer's real output rather than from a failing test: a checked item that **nests a sub-list** would have struck its children through too, because remark nests the child `ul` INSIDE the parent `li` (excluded with `:not(:has(ul,ol))`; the same nesting ruled out `display:flex` on the item, which would have laid checkbox, label and sub-list out as siblings in a row), and a **loose list** — one with blank lines between items — wraps each label in a `<p>`, moving the checkbox from `li > input` to `li > p > input` where the first selector matched nothing, leaving a checked item unstruck. `src/styles.css` has the identical gap and was left alone by decision: it is latent, since nothing in `src/` renders with `className="markdown"` today. `scripts/shared-render.test.mjs` is new — the renderer had no test file; it asserts against the returned page, which carries markup and stylesheet in one string, so a rendering rule is verifiable end to end without a browser. ADR 0025 already `Implemented`, no status change, no INDEX change. 121 → 130 tests. No version bump — rides along with a later release. See `plan/done/2026-08-06-shared-task-lists-show-bullet-and-checkbox.md`. |
| 2026-08-06 | 7f192d8, d8cb7c2 | main | ADR 0037 — versioning policy + framework version in the build | Revised the versioning decision before implementing it. 0037 was "semver, 1.0.0, GitHub Releases"; Releases and a moving `latest` ref are now out — both add a per-release ceremony and a second source of truth that can drift from the tags, and a moving ref resolves to a commit SHA rather than a version, so naming the version still means reading the tag list. The tags are already public, machine-readable, and the thing adopters pin. The `1.0.0` commitment went too: the major has no mechanical consequence here (a git-tag dependency's lockfile pins a resolved commit either way), so freezing the contract stays a future decision. What replaced them: the semver policy inline, including the 0.x rule that breaking changes ride the minor; the framework version baked into the build; a status-bar indicator; and AC3, that every version reference moves with the tag. Implementation — `frameworkVersion` read from THIS package's `package.json` via `PACKAGE_DIR`, never `PROJECT_DIR`, which is the consumer's `.web` and would bake the adopter's shell version: a wrong number, and invisible in this repo's own checkout where the two directories coincide. Verified in the case this repo cannot reproduce (throwaway consumer, `portal:` link, `wv dev` from its `.web`): vault commit `dd428b7` dirty vs framework `0.6.1`, different values from different sources. The status bar renders the version as its own element outside the commit anchor — it is not a property of that commit and has nowhere to link to — and both tooltips now name what they describe; the commit's said just "Build abcdef1", ambiguous the moment a version sits beside it. Both regressions confirmed caught, not merely covered: `PACKAGE_DIR` → `PROJECT_DIR` fails the build test, removing the indicator fails 3 of 5 status-bar tests (the other 2 assert the degradation on builds predating the field). Also `AGENTS.md` gained "Cutting a release": four places name a version (`package.json`, tag, `SETUP.md` pin, `README.md` blob link) and nothing fails when they drift — at `v0.5.0` the `SETUP.md` pin was two releases stale and was caught by chance. Left as documented process, not a gate check: one drift is not yet a reason for machinery. Rendering confirmed in a browser by the owner. 0038 r3 and 0039 r2 realigned onto tags in the same pass. 130 → 137 tests. See `plan/done/2026-08-06-framework-version-in-build-and-status-bar.md`. |
| 2026-08-06 | ec34c7b, 07173a2 | main | ADR 0038 — in-app upgrade notice (plan todo/0005) | The portal now tells an adopter when a newer WebVault is published. Reads the framework repository's `/tags`, selects the highest by numeric semver comparison, and compares strictly-newer against `build.frameworkVersion`. Both of those are load-bearing and neither reproduces locally: `/tags` is **lexicographic**, so today's `v0.6.1, v0.6.0, v0.5.4, …` is correctly ordered only because every minor is one digit — it stays right until `v0.10.0` and then silently is not; and a build can be AHEAD of every tag (a maintainer on `main` after tagging), where an inequality test would show a permanent false notice. The repo to query is a constant in framework code: `build.repo` is the ADOPTER's vault, the same identity trap as `PACKAGE_DIR`/`PROJECT_DIR` the day before. **Two real defects caught by the tests, not after them.** The throttle guard read `Number(state.checkedAt) || 0`, collapsing "never checked" into "checked at the epoch", so the FIRST check was refused as though one had just happened — on a fresh install the notice would never have appeared, and hand-testing cannot see it because the timestamp already exists by the time anyone looks. And `run()` recorded `Date.now()` while ignoring the injected clock, making every window look fresh and the cadence untestable. Cadence: one automatic check per hour, manual re-check floored at a minute, both persisted — far inside the unauthenticated 60/hour. Every failure path silent (network, non-2xx, rate limit, malformed payload, no usable tags): a failed check is "no notice this time", and the timestamp is still written so a down endpoint is not retried per render. UI: green dot plus an anchored panel (`.statuspanel`'s pattern), extracted into `VersionIndicator.jsx` rather than grown inside `StatusBar.jsx`. The panel links to `/tree/vX.Y.Z`, whose README renders — `/releases/tag/…` is near-empty since ADR 0037 publishes no Releases. **Deviation after seeing it run:** the Dismiss button is gone at the owner's request. It persisted a per-version dismissal and so hid the dot for good, the suppression outliving the click with no way back; dismissing now closes the panel and the dot stays. ADR 0038 r5 clarified criterion 5 accordingly — it always meant the panel closes. Verified against the live endpoint and in a browser by the owner, with a throwaway consumer temporarily at `0.5.0` so the real `v0.6.1` registered as newer. 137 → 159 tests. See `plan/done/2026-08-06-in-app-upgrade-notice.md`. |
| 2026-08-06 | 18b4511, v0.8.0 | main | ADR 0039 — one-click upgrade from the notice (plan todo/0004) | The upgrade loop closes: the notice from `0038` gains an action, so a deployment that can write upgrades itself instead of sending the adopter to hand-edit a pin. The bump is a commit like any other — it targets the branch the deployment was built from (`adr/0020`), so the push rebuilds and the rebuild reinstalls; no separate reinstall step exists to orchestrate. **A dedicated `/api/upgrade`, not a widened `/api/commit`.** `isSafeNotePath` rejects `.web/package.json` three times over (needs `.md`, no dot-segments, and its comment says so) and that guard is load-bearing: it keeps the note editor away from the toolchain. The new endpoint has one file and one field in its entire write scope; a test asserts `/api/commit` still cannot reach the shell, and if it ever fails the editor has gained write access to the build config. **The capability signal is runtime, and that is the trap.** There was no client notion of "can this deployment write" — every caller of `commitFiles` attempted the commit and learned from a 500, which is fine for a button the user pressed but useless for choosing which affordance to render before any click. It had to be runtime rather than build-injected: the token is a host secret adopters add AFTER deploying (the welcome note instructs exactly that), so a build-time flag would report `false` on a deployment that writes perfectly well until the next rebuild. This answers the open question `adr/0034` had explicitly parked for its own plan; 0034 now consumes the endpoint instead of defining it (r2), its editor gating still unbuilt and unchanged in scope. **The deployed version had to become fetchable.** Exit criterion 7 needs to observe the rebuild landing and there was nothing to observe — no `build-info.json` exists and the framework version is compiled into the bundle, so a client polling would have watched a number that cannot change underneath it, or 404'd forever while silently treating each failure as "still building". The version is now build-injected into the Worker config and reported beside `canWrite`. The poll is bounded at 15 minutes: a rebuild can fail, and a spinner that never ends is worse than a stated timeout; a timeout reports "still building", not failure, because the commit landed either way. The pin rewrite is a targeted string replacement, NOT `JSON.parse`/`stringify`, which would reformat the adopter's own file and turn a one-line upgrade into a whole-file diff in their history; the target is validated as a published tag before anything is written. **Local parity**, requested by the owner so the flow is testable without a deployment: `capabilities-dev.mjs` and `upgrade-dev.mjs` mirror the `commitDev` pattern, with two deliberate differences — `canWrite` is true regardless of any token, because local writes go to disk and no token authorizes them (`adr/0036`), and the response carries `local: true` so the client says "reinstall to pick it up" rather than polling for a rebuild dev will never perform. Tag validation is NOT skipped locally: dev must not accept a target production would refuse. Verified end-to-end against the live GitHub API before release — unpublished tag and malformed version both refused without writing, a real tag rewriting exactly one line with the rest of the manifest byte-identical. 159 → 190 tests. Also carries what had accumulated on `main` since `v0.6.1` for a `v0.7.0` that was never cut (ADRs `0025`, `0037`, `0038`). See `plan/done/2026-08-06-one-click-upgrade-from-the-notice.md`. |
| 2026-08-06 | c2cd6e0 | main | ADR 0015 r7 — link passes must skip code (plan todo/0003) | Reversed a wrong downgrade and fixed the defect behind it. `preProcessWikilinks` and `preProcessMediaLinks` matched across line boundaries and did not skip code, so an unmatched `[[` in a code span — the shape a note documenting the wikilink syntax naturally has — started a match running to the next `]]` anywhere later in the note, collapsing every heading and paragraph in between into one inline token. Four blocks became two. **The reason this stayed open for two audits is the interesting part:** each post pass reverses its own pre pass symmetrically, so the bytes round-trip perfectly and a bytes-only check reports no defect — which is exactly what the 2026-08-05 audit ran before downgrading the item to "test coverage, not a fix". The loss was in the block structure the editor displays, and it became permanent the moment the user edited the mangled block and committed the encoded blob as literal text. Fix: both matchers bounded to one line, plus a shared `outsideCode` helper that applies a replacer only outside fenced blocks and inline code spans. **The pipeline order stayed put, against what the plan item assumed.** Reordering `preProcessFences` first would not have protected fence content — it rewrites only the opening fence line into a sentinel and leaves the block body untouched — and no ordering can protect an inline code span, since nothing marks one; the pass had to learn both itself either way. Exit criterion 5 verified by breaking it: `postProcessWikilinks` temporarily emitting `[x]` fails 10 tests. The three structural tests were confirmed failing against the unmodified tree. ADR `0015` gains criterion 7, stating that the block structure is part of the contract and not only the bytes, so the next pre-process pass is written against the rule rather than rediscovering it. Left alone deliberately: `bodyHasUnsafeForBlockNote` runs `MD_LINK` over the whole body without skipping code, so a note merely *mentioning* a media link in a fence still routes to the raw editor — a false positive in the safe direction. 190 → 210 tests. |
| 2026-08-06 | 6648187 | main | ADR 0038 r7 — visible outcome for the manual update check (plan todo/0001) | Reopened a shipped ADR: `0038` back to **Accepted**. The notice was verified along the update-available path; the common path — clicking the version to confirm you are current — renders nothing, since a check finding no update writes only `checkedAt` and `VersionIndicator` never reads it. Throttle-refused and in-flight are equally invisible. Two more defects fell out: AC6's blanket silence would make a *failed* check report "up to date", and the update dot is green, the colour this app uses for `In sync`. AC6 qualified by mode, AC8 makes a throttle refusal unobservable, new AC9 (pending + exactly one terminal outcome), AC10 (colour semantics), AC11 (in-app tooltip with last-check time). Decision only; no implementation. Queued as `plan/todo/0001-upgrade-check-feedback.md`. |
