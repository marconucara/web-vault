# wv CLI — dev/build/preview

Owning ADR: `adr/0029-cli-setup-and-distribution.md` (partial — the CLI only).

The `wv` bin (`dev`/`build`/`preview`) wrapping the Vite pipeline, plus the
local-linking mechanics it absorbs so the user configures nothing: preserve-symlinks
for itself and spawned children, `execArgv` propagation, Vite `preserveSymlinks`.

Shipped: initial import of web-vault (backfill; implementation predates the ADR).
