# Atomic multi-file commit via the GitHub Git Data API

Owning ADR: `adr/0019-atomic-commit-git-data-api.md`.

Pre-existing capability, recorded as an ADR after the fact (backfill).

Shipped: initial import of web-vault (backfill; implementation predates the ADR).
