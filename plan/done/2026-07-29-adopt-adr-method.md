# Adopt the ADR / docflow method

Owning ADR: `adr/0001-record-architecture-decisions.md`.

Scope: bootstrap the documentation-led, ADR-driven convention set in this repo
— `AGENTS.md`, `CLAUDE.md`, `CONVENTIONS.md`, the ADR template and catalogue,
the `plan/` queue, and the `_agent/` coordination files — and backfill the
existing decision history into ADRs.

Exit criteria: the core docflow files exist at the repository root; the seed ADR
0001 is recorded as Implemented; `INDEX.md` lists the catalogue.

Shipped: bootstrap of the convention set (this repository's first commit).
